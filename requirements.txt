# Flask web framework
# Used for routing, templating, and request handling
Flask==3.0.0

# Gunicorn WSGI server
# App Engine uses this to serve the Flask app in production
gunicorn==21.2.0

# Werkzeug is a Flask dependency but we pin it for security
Werkzeug==3.0.1

# Google OAuth libraries
# Official Google libraries for OAuth 2.0 authentication
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# Flask session management
# Server-side session storage for Flask
Flask-Session==0.6.0

# Google Cloud Datastore
# NoSQL database for storing session data and application data
google-cloud-datastore==2.19.0

# Google Calendar API
# Used to create calendar events for booked camps and registration reminders
google-api-python-client==2.111.0

# Date utilities
# Used for parsing and formatting dates in calendar integration
python-dateutil==2.8.2

# Google Vertex AI for Gemini
# Used for AI-powered parsing of camp website URLs (using Gemini 2.5 Flash)
google-cloud-aiplatform==1.70.0

# HTML parsing for web scraping
# Used to extract and parse camp website content
beautifulsoup4==4.12.3

# HTTP library for fetching URLs
# Used to download camp website HTML
requests==2.32.3
